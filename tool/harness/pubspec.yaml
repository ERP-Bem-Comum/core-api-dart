name: harness
description: >-
  Ferramentas do repositório: hooks do Claude Code, validadores do portão, gerador do
  README e publicação do diário. Compilado em um único executável.
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  args: ^2.7.0
  yaml: ^3.1.2

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.0
